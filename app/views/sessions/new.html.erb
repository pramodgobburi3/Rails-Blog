<p id="alert"><%= alert %></p>


 <style>
    .form-signin {
      width: 100%;
      max-width: 330px;
      padding: 15px;
      margin: auto;
    }
    .form-signin .checkbox {
      font-weight: 400;
    }
    .form-signin .form-control {
      position: relative;
      box-sizing: border-box;
      height: auto;
      padding: 10px;
      font-size: 16px;
    }
    .form-signin .form-control:focus {
      z-index: 2;
    }
    .form-signin input[type="email"] {
      margin-bottom: -1px;
      border-bottom-right-radius: 0;
      border-bottom-left-radius: 0;
    }
    .form-signin input[type="password"] {
      margin-bottom: 10px;
      border-top-left-radius: 0;
      border-top-right-radius: 0;
    }
    .center-screen {
        width: 100%;
        max-width: 330px;
        align-items: center;
        padding: 15px;
        margin: 0 auto;
    }

    box-body {
        height: 100%;
    }

    box-body {
        display: -ms-flexbox;
        display: -webkit-box;
        display: flex;
        -ms-flex-align: center;
        -ms-flex-pack: center;
        -webkit-box-align: center;
        align-items: center;
        -webkit-box-pack: center;
        justify-content: center;
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
    }
  </style>
<div class="box-body">
    <div class="center-screen">
        <%= form_tag sessions_path do |form| %>

            <% if @show_error == true %>
                <div class="alert alert-danger">"The Email or Password is incorrect"</div>
            <% end %>
            
            <div class="form-signin">
                <div style="display: flex; justify-content: center;">
                    <img class="mb-4" src="/images/icon.png" alt="logo" width="72" height="72" >
                </div>
                <h2 style="text-align: center">Login</h1>
                <div class="field" style="padding-bottom:5px">
                    <%= text_field_tag :email, "", class: "form-control", placeholder: "Email" %>
                </div>
                <div class="field">
                    <%= password_field_tag :password, "", class: "form-control", placeholder: "Password" %>
                </div>

                <div class="actions">
                    <%= submit_tag "Login", :class => "btn btn-primary btn-block" %>
                </div>
                <br>
                <div class="actions">
                    <%= link_to "Create an account", signup_path, :class => "btn btn-outline-dark btn-block" %>
                </div>
            </div>
        <% end %>
    </div>
</div>